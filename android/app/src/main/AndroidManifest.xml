<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 高德定位所需权限 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Mock Location：把自己注册为"模拟位置应用"。
         Android 6.0+ 同时依赖 AppOpsManager.OPSTR_MOCK_LOCATION，
         需用户在"开发者选项→选择模拟位置应用"中选定本 APP。
         Android 4.x 及之前的 Settings.Secure.ALLOW_MOCK_LOCATION
         已在 M (23) 弃用，但 manifest 这里写不区分版本。
         授权后 addTestProvider 才会成功。
         注意：不要写 android:mockLocation 属性，该属性并非 framework 官方属性，
         AAPT 会报 "attribute not found"。-->
    <uses-permission android:name="android.permission.ACCESS_MOCK_LOCATION" />
    <!-- 前台服务保活：mock 位置需在后台持续推送，避免退后台被系统冻结 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- WakeLock：强制 CPU 不休眠，对抗华为鸿蒙对后台进程的 FREEZE 冻结 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:label="showlocation"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowNativeHeapPointerTagging="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- 前台服务：后台持续注入 mock 位置 -->
        <service
            android:name=".MockLocationService"
            android:exported="false"
            android:foregroundServiceType="location" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
