name: csp_amap_flutter_map
description: |
  A Flutter plugin for AMap (Gaode Map) SDK with support for Android and iOS platforms.
  This is the pub.dev compatible version. For full HarmonyOS support, please visit our Gitee repository.
  Features include map display, markers, polylines, polygons, location services, POI search, API key
  management, privacy compliance, and custom styling. The base library (csp_amap_flutter_base) is
  bundled into this package (lib/base/), so no extra dependency is required.
version: 1.1.1
homepage: https://gitee.com/chenshipeng0914/csp_amap_flutter_map
repository: https://gitee.com/chenshipeng0914/csp_amap_flutter_map
issue_tracker: https://gitee.com/chenshipeng0914/csp_amap_flutter_map/issues
documentation: https://gitee.com/chenshipeng0914/csp_amap_flutter_map/blob/develop/README.md
# publish_to: 'none' # Remove this line to publish to pub.dev

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_plugin_android_lifecycle: ^2.0.21
  meta: ^1.3.0
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.16.5
  mockito: ^5.4.4
  flutter_lints: ^3.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: cn.csp.csp_amap_flutter_map
        pluginClass: CspAmapFlutterMapPlugin
      ios:
        pluginClass: CspAmapFlutterMapPlugin
      ohos:
        package: cn.csp.csp_amap_flutter_map
        pluginClass: CspAmapFlutterMapPlugin

  # To add assets to your plugin package, add an assets section, like this:
  assets:
    - assets/
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/assets-and-images/#from-packages
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # To add custom fonts to your plugin package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/custom-fonts/#from-packages
